"use client";
import { Button } from "antd";

export default function HomeCompo() {
    return (
        <div>
            <Button onClick={() => alert("hello")}>xin chào</Button>
        </div>
    );
}
